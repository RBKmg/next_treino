import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Minha conta',
}

export default function ContaPage() {

    return (
        <div>
            <h1>Minha conta</h1>
        </div>
    )
}

